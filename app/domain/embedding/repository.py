"""
Embedding Repository Interface
"""
from abc import ABC, abstractmethod
from typing import Optional, List

class EmbeddingRepository(ABC):
    """Repository interface for embedding domain"""
    pass
