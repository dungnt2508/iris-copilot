"""
Generation Repository Interface
"""
from abc import ABC, abstractmethod
from typing import Optional, List

class GenerationRepository(ABC):
    """Repository interface for generation domain"""
    pass
